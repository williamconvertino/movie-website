//Note the capitalized file name
export default function ExampleComponent () {
    
    const val = 1 + 1;
    //You can do your code in the function
    
    return (
        <div>
            This is the example component's HTML. The value is {val}.
            {/* You can put javascript code in curley brackets, like "val" above */}
        </div>
    )
}